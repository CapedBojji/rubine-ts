--!strict
return {
    scheduler = require(script:FindFirstChild("scheduler")),
    abstractions = require(script:FindFirstChild("abstractions")),
    world = require(script:FindFirstChild("world")),
}
